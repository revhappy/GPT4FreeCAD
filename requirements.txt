# GPT4FreeCAD has NO third-party Python dependencies.
# It uses only the standard library (urllib, json, ssl) plus the FreeCAD-bundled
# PySide2/PySide6. Nothing to pip install - this file documents that fact.
